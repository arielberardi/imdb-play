# Environment Variables

# Logging
LOG_LEVEL="info"

# Database
DATABASE_URL="postgresql://imdb:imdb@localhost:5432/imdb_play?schema=public"

# Security
SESSION_SECRET="minimum_32_character_secret_for_cookie_signing_security"

# Optional: Redis-backed distributed rate limiting (Upstash REST API)
# Local Redis from docker-compose is available at redis://localhost:6379
# (current rate limiter uses Upstash REST env vars below for distributed mode)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
